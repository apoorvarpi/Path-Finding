function send(A)
%SEND - save in a file named path.txt
%   Detailed explanation goes here
    fileID = fopen('path.txt','w');
    for i=1:size(A,1)
        disp ([A(i,1) A(i,2)]);
        fprintf(fileID,'%d %d\n', A(i,1), A(i,2));
    end;
    fclose(fileID);
end

